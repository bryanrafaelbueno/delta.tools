{
  "name": "delta-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Delta.tools - private, ad-free, client-side file conversion with a community plugin marketplace",
  "type": "module",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c yellow,cyan \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
